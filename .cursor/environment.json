{
  "name": "Soyla — Telegram Mini App + LiveKit",
  "user": "ubuntu",
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "api",
      "command": "cd /workspace && . .venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
      "description": "FastAPI backend + Telegram Mini App (http://localhost:8000, webapp at /webapp/, health at /health)."
    },
    {
      "name": "bot",
      "command": "bash /workspace/.cursor/run_bot.sh",
      "description": "aiogram Telegram bot. Idle until a real BOT_TOKEN secret is provided."
    }
  ],
  "ports": [
    { "name": "api", "port": 8000 }
  ]
}
